.nav {
    display: flex;
    justify-content: flex-start;
    align-items: center;

    &__link {
        color: white;
        font-size: rem(16px);
        font-weight: 400;
        text-decoration: none;

        &:not(:last-child) {
            margin-right: rem(60px);
        }
    }
}
